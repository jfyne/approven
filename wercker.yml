box: node
build:
  steps:
    - npm-install
    - npm-test

    - hgen/gulp:
        tasks: build

    - script:
        name: echo nodejs information
        code: |
          echo "node version $(node -xv) running"
          echo "npm version $(npm -xv) running"
          
          
deploy:
  steps:
      - michilu/gcs-website-deploy:
        bucket: www.approven.com
        project: $GOOGLE_PROJECT_ID
        token: $GOOGLE_REFRESH_TOKEN
        dir: build
        
        
